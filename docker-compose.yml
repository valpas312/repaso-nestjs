version: '3.8'

services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: valpas312
      POSTGRES_PASSWORD: valpas312pass
      POSTGRES_DB: nestjsdb
    ports:
      - "5432:5432"